<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lab2_g1_h1091">
    <!--
    Agregados:
    - Permite salidas a Internet
    - Consultas a http
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".formularioEmpleado"
            android:label="@string/formulario_empleado_title">

        </activity>
        <activity
            android:name=".formularioTrabajo2"
            android:label="@string/formulario_trabajo_title"/>
        <activity
            android:name=".EmpleadosActivity"
            android:label="@string/empleados_activity_title" />
        <activity
            android:name=".MainActivity"
            android:label="@string/main_activity_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>